import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    loadingUser: {
        flex: 1,
        justifyContent: 'center',
    },
    card:{
        alignItems: 'center',
    },
    posButton: {
        width: '100%',
        justifyContent: 'center',
    },
    buttonText: {
        color: 'white',
    },
    errorText: {
        textAlign: 'center',
    },
    retrieveUser: {
        justifyContent: 'center',
        width: '40%',
        alignSelf: 'center',
        marginTop: 10,
    }
});